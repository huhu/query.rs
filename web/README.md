
# Run locally

```
$ git clone --recursive git@github.com:huhu/query.rs.git

$ curl -sSfL https://github.com/mitsuhiko/minijinja/releases/latest/download/minijinja-cli-installer.sh | sh

$ cd web

$ make build

$ npm install

$ npx wrangler pages dev
```

